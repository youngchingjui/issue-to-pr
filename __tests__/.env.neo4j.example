# Neo4j Integration Test Configuration
# Copy this file to .env.neo4j
#
# Uses the shared test infrastructure (same as e2e tests).
# Start with: pnpm test:infra:up
# Stop with:  pnpm test:infra:down

# Neo4j connection (from docker/docker-compose.e2e-test.yml)
NEO4J_URI=bolt://localhost:17687
NEO4J_USER=neo4j
NEO4J_PASSWORD=e2e-test-password
