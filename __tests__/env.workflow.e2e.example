# Example env file for manual E2E workflow test
# Copy this to __tests__/.env.workflow.e2e and fill in real values.

# Required for the test
E2E_REPO_FULL_NAME=youngchingjui/openai-realtime-agents-test-playground
E2E_ISSUE_NUMBER=123
E2E_GITHUB_LOGIN=your-github-username

# Optional (branch to use for the workflow run)
E2E_BRANCH=feature/e2e-test-branch

# The services below must be running in your environment before executing the test:
# - Redis (set REDIS_URL)
# - Neo4j (NEO4J_URI, NEO4J_USER, NEO4J_PASSWORD)
# - Workflow worker (see apps/workers/workflow-workers)
# - GitHub App credentials for the worker
# - OpenAI API key
# These are typically configured outside this file (e.g., in your shell or
# Docker Compose env files), but are listed here as a reminder.
# REDIS_URL=redis://localhost:6379
# NEO4J_URI=bolt://localhost:7687
# NEO4J_USER=neo4j
# NEO4J_PASSWORD=password
# GITHUB_APP_ID=123456
# GITHUB_APP_PRIVATE_KEY_PATH=/absolute/path/to/github-app-private-key.pem
# OPENAI_API_KEY=sk-...
